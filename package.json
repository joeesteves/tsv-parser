{
  "dependencies": {
    "request": "^2.83.0",
    "rxjs": "^5.5.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "scripts": {
    "start": "yarn run build && node lib",
    "build": "babel src -d lib"
  },
  "name": "tsv-parser",
  "version": "2.0.3",
  "main": "lib/index.js",
  "license": "MIT",
  "author": {
    "name": "Jose Esteves",
    "url": "https://github.com/ponyesteves"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ponyesteves/tsv-parser"
  },
  "description": "Simple TSV to JSON parse. Uses the first line as headers and returns an observable"
}
